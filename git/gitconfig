[user]
	name = Josh Langsfeld
	email = jdlangs@gmail.com
[credential]
	helper = cache --timeout=7200
[alias]
	br = branch
	out = status
	st = status
	ci = commit
	rb = rebase
	co = checkout
	serve = daemon --verbose --export-all --base-path=.git --reuseaddr --strict-paths .git/
    who = config --get user.email
	root = rev-parse --show-toplevel
[github]
	user = jdlangs
[push]
	default = simple
[rerere]
	enabled = 1
[core]
	editor = nvim
[diff]
	tool = meld
